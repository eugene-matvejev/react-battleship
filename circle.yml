machine:
    node:
        version: 6.9

dependencies:
    cache_directories:
        - ~/.npm/

test:
    pre:
        - npm install -g codecov
        - npm run generate:all
    override:
        - npm test -- --coverage
        - codecov
