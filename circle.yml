machine:
    node:
        version: 8.9

dependencies:
    cache_directories:
        - ~/.npm/
    override:
        - npm i --no-progress

test:
    override:
        - npm test -- --coverage
        - codecov
