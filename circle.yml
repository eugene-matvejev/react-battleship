machine:
    node:
        version: 6.9

dependencies:
    cache_directories:
        - ~/.npm/

test:
    pre:
        - npm install -g codecov
    override:
        - npm test -- --coverage
        - codecov
