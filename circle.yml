machine:
    node:
        version: 6.9

dependencies:
    cache_directories:
        - ~/.npm/
    override:
        - npm i --no-progress
    post:
        - npm i -g codecov

test:
    override:
        - npm test -- --coverage
        - codecov
